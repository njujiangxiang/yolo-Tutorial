# API 密钥 (可选)
ROBOFLOW_API_KEY=your_api_key_here

# 训练配置
IMG_SIZE=640
BATCH_SIZE=16
EPOCHS=100

# 设备配置
DEVICE=0  # GPU ID, -1 for CPU
WORKERS=8

# 项目配置
PROJECT=runs/detect
NAME=experiment
EXIST_OK=False
